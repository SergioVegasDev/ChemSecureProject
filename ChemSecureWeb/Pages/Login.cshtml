@page
@model ChemSecureWeb.Pages.LoginModel
@{
    ViewData["Title"] = "Login";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/login.css" />
</head>
<body>
    <div class="login-container">
        <form method="post" class="login-form">
            <h2>Login</h2>

            <div asp-validation-summary="All" class="text-danger"></div>

            <div>
                <label asp-for="Login.Email">Email</label>
                <input asp-for="Login.Email" type="text" placeholder="Insert Email" />
                <span asp-validation-for="Login.Email" class="text-danger"></span>
            </div>

            <div>
                <label asp-for="Login.Password">Password</label>
                <input asp-for="Login.Password" type="password" placeholder="Insert Password" />
                <span asp-validation-for="Login.Password" class="text-danger"></span>
            </div>

            <button type="submit">Submit</button>
        </form>
    </div>

    <partial name="_ValidationScriptsPartial" />
</body>
</html>